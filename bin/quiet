#!/bin/bash

cd "$(dirname "$0")/.."
quiet_path=$(python3 -c "import settings; print(settings.QUIET_PATH)")

if [[ $(cat "$quiet_path") == "" ]]; then
	echo -e "\n\tQuiet Mode On\n"
	echo "Quiet" > "$quiet_path"
else
	echo -e "\n\tQuiet Mode Off\n"
	echo -n "" > "$quiet_path"
fi
